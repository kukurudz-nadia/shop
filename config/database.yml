default: &default
  adapter: postgresql
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: postgres
  password: admin
  host: localhost
  port: 5433

development:
  <<: *default
  database: shop_development

test:
  <<: *default
  database: shop_test