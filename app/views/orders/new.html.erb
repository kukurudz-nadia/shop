<div class="max-w-5xl mx-auto">
    <div class="w-full max-w-sm bg-amber-100 border border-gray-200 rounded-lg shadow dark:bg-gray-800 dark:border-gray-700">
      <div class="px-5 pb-5">
      <p class="py-2.5 text-2xl font-bold w-4/5 mx-auto">ORDER</p>
        <%= simple_form_for @order do |form| %>
          <%= form.simple_fields_for :order_detail do |detail| %>
            <%= detail.input :first_name %>
            <%= detail.input :last_name %>
            <%= detail.input :email %>
            <%= detail.simple_fields_for :address do |address| %>
              <%= address.input :country , as: :text %>
              <%= address.input :city %>
              <%= address.input :street %>
              <%= address.input :comment %>
            <% end%>
          <% end %>
            <div>
              <%= form.submit "Submit",  class:"btn-green" %>
            </div>
          </div>
        <% end %>
    </div>
</div>